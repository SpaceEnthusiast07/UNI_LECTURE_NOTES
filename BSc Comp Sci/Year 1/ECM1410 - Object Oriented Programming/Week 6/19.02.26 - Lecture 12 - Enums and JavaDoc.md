[Slides](https://ele.exeter.ac.uk/pluginfile.php/5443535/mod_folder/content/0/L12-25-enum.pdf?forcedownload=0)


##### Contents
 - [[#Enums]]
	 - [[#What is an Enum?]]
	 - [[#Using Enums]]
	 - [[#Enum Behaviour]]
 - [[#Javadoc]]
	 - 


#### Enums
##### What is an Enum?
 - An `enum` type/class is a special reference type that holds a set of predefined constants.
 - Example definition structure in Java:
```java
enum Day {
	SUNDAY, MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY
}
```
 - By convention, all variable names are capitalised.
 - All constants in an `enum` are defined implicitly as `public`, `static`, and `final`.


##### Using Enums
 - You do not instantiate an `enum` class/type.
 - They are pre-allocated and fixed.
 - Example: `Day d = Day.SUNDAY;`.
 - To get all the values of an `enum`: `Day[] allDays = Day.values();`.
	 - Each `enum` type has a static method `values()`.
 - Example Java program from slides:
```java
// A demo to comment according to which day today is
enum Day { // The values are separated by comma
	SUNDAY, MONDAY, TUESDAY, WEDNESDAY,
	THURSDAY, FRIDAY, SATURDAY
}

public class EnumApp {
	public static void main(String[] args) {
		Day today = Day.FRIDAY;
		// Output the day
		System.out.println("Today is "+today+"\n");
		for (Day d:Day.values()) // List all seven days
		giveComments(d);
	}

	public static void giveComments(Day day) {
		switch (day) {
		case MONDAY:
		System.out.println(day+": Mondays are bad.");
		break;
		case FRIDAY:
		System.out.println(day+": Fridays are better.");
		break;
		case SATURDAY: case SUNDAY:
		System.out.println(day+": Weekends are best.");
		break;
		default:
		System.out.println(day+": Midweek days are so-so.");
		}
	}
}
```


##### Enum Behaviour
 - Enums can behave like normal classes.
 - They can have other attributes and methods. ![[Pasted image 20260219145801.png]]


#### Javadoc
##### Java Documentation
 - Comments are used to explain code and aid maintainability.
 - Single line comment: `// ...`.
 - Multi-line comment: `/* ... */`.
 - Documentation comment: `/** ... */`.


##### What is Javadoc?
 - Javadoc is a JDK tool that automatically creates fancy *HTML-based documentation* based on the documentation comments (`/** ... */`).
 - Example output: ![[Pasted image 20260219151252.png]]
 - Before each class, you must provide a "doc string" (documentation comment) that gives a description for that class. ![[Pasted image 20260219151341.png]]


##### Doc Tags
 - You can include special doc tags in the doc strings that provide specific information used by Javadoc to format the documentation pages.
 - `@author` - Provides information about the author.
 - `@version` - Indicates the version number.
 - `@since` - Indicate the version.
 - `@param` - Provides the name and description .
 - `@return` - Provides information about the author.
 - `@throws` - Provides information about the author.





